<h1>Questions!</h1>

<ul>
  <% @questions.each do |question| %>
    <li><%= link_to question.text, question_path(question), class: "question" %></li>
  <% end %>
</ul>

<% if current_user.nil? == false %>
  <%= link_to "Ask Question", new_question_path %>
<% end %>
